name: Sync Quark APK → GitHub Release → Telegram

on:
  workflow_dispatch:          # 手动触发
  schedule:
    - cron: "0 */6 * * *"    # 每6小时自动触发

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests PyGithub

      - name: 下载最新 APK
        env:
          QUARK_STOKEN: ${{ secrets.QUARK_STOKEN }}
          QUARK_ROOT_FID: ${{ secrets.QUARK_ROOT_FID }}
        run: |
          python monitor_worker.py

      - name: 上传到 GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          # 这里写你上传 apk/ 文件到 Release 的逻辑

      - name: 推送到 Telegram
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          BOT_API_BASE: ${{ secrets.BOT_API_BASE }}
        run: |
          # 这里写你用 monitor_worker.py 下载的 apk/ 文件推送的逻辑
